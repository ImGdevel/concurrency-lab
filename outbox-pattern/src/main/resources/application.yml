server:
  port: 8088
spring:
  application:
    name: outbox-pattern
  data:
    redis:
      host: localhost
      port: 6388
  datasource:
    url: jdbc:mysql://localhost:3318/outbox_pattern?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: lab
    password: lab1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
